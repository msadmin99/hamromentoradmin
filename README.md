# Hamromentor — Admin

Next.js admin panel (content & user management), calling the same Django
API as the Frontend. See the top-level `../README.md` for setup,
architecture, and demo credentials.

```bash
npm install
npm run dev   # http://localhost:3001
```
